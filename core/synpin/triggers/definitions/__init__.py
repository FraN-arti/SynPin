"""
Triggers — `idle_head` plugin (definitions package marker).

This file marks the definitions/ subpackage. Each plugin lives as
two files with the same basename: `<type>.yaml` (metadata, UI-facing)
and `<type>.py` (execution, engine-facing). The registry scans this
directory and pairs them.
"""
